{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'cmd=\"${CLAUDE_TOOL_INPUT_COMMAND:-}\"; if [[ \"$cmd\" =~ git[[:space:]]+push ]]; then bash \"$(git rev-parse --show-toplevel)/.claude/hooks/pre-push\" </dev/null; fi'"
          }
        ]
      }
    ]
  }
}
