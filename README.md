# gh-pages

This branch is used by [benchmark-action/github-action-benchmark](https://github.com/benchmark-action/github-action-benchmark) to store per-commit benchmark trend data for curry's CI (see .github/workflows/benchmark.yml on main). It has no relation to main's history and should not be merged into it.
