# `(srfi s132 sorting)` and `(srfi s133 vectors)` — SRFI-132 / SRFI-133

```scheme
(import (srfi s132 sorting) (srfi s133 vectors))
(list-sort < '(3 1 2))              ; => (1 2 3)
(vector-fold + 0 #(1 2 3 4))        ; => 10
(vector-binary-search #(1 3 5 7 9) 5 <) ; => 2
```

[SRFI-132](https://srfi.schemers.org/srfi-132/): `list-sorted?`/`vector-sorted?`, `list-sort`/`list-stable-sort`(`!`), `vector-sort`/`vector-stable-sort`(`!`), `list-merge`(`!`), `vector-merge`(`!`). Every sort is the same stable merge sort — the SRFI permits `list-sort`/`vector-sort` to be unstable, but there's no benefit to a second, less-predictable algorithm, so the "plain" and "stable" names are aliases. `vector-find-median`/`vector-select!`/`vector-separate!` from the full SRFI are out of scope.

[SRFI-133](https://srfi.schemers.org/srfi-133/) adds, on top of curry's native R7RS vector ops (`make-vector`, `vector-copy`(`!`), `vector-append`, `vector-map`/`-for-each`, etc.): `vector-empty?`, `vector=`, `vector-swap!`, `reverse!`/`vector-reverse!`(`*`), `vector-index`(`-right`), `vector-count`, `vector-any`/`-every`, `vector-fold`(`-right`), `vector-binary-search`, `vector-concatenate`, `vector-unfold`(`-right`).

## See also

- [`index.md`](index.md) — full SRFI availability table
